import { render, screen } from "@testing-library/react";
import App from "./App";

// Skip this test due to Typescript error
// Cannot use import outside of a module
// Skip didnt work :D
// describe("App.tsx", () => {
//   it("Renders the title of the game", () => {
//     render(<App />);
//     const titleElement = screen.getByText(/FDIPARDY/i);
//     expect(titleElement).toBeInTheDocument();
//   });
// });
